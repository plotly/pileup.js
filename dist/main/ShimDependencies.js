/**
 * Shim Dependencies
 * 
 */
'use strict';Object.defineProperty(exports, '__esModule', { value: true });

var React = window && window.React || global && global.React || require('react');exports.React = React;
var ReactDOM = window && window.ReactDOM || global && global.ReactDOM || require('react-dom');exports.ReactDOM = ReactDOM;